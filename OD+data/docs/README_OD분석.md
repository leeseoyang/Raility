# OD 분석 모듈 기여 개요 (add-od-and-data 브랜치)

README '한계' 항목 **"실제 OD가 아닌 운행빈도 프록시 사용 — 시간대별 수요 편차는
반영되지 않음"** 을 해소할 수 있는 실측 데이터·모듈·문서를 추가했다.
**기존 파일은 하나도 수정하지 않았고** 전부 새 파일이다. 기존 파이프라인
(analyze.py 등)에의 반영 여부·방식은 저자 판단에 맡긴다.

## 무엇이 추가됐나

### 1. 데이터

- `data/raw/역간거리/` +28개(역간거리 25·호선구성역정보 3), `data/raw/환승정보/` 15종 (신규 폴더)
  — 출처·활용 제안은 `data/raw/README_추가데이터.md`
- `data/od/` (신규) — 서울시 역사마스터, 부산·대구·대전·인천 승하차, 서울 월별
  승하차 대표 2개월. **서울시 역간 OD zip 7개(66MB)는 저장소 컨벤션에 따라
  용량 제외** — 다운로드 안내는 `data/od/README.md`, `*.zip` 은 `data/od/.gitignore` 처리.

### 2. OD 분석 모듈

- `od_analysis.py` (저장소 루트) — 서울시 역간 OD(교통카드, 2026-07-22~28 7일치)와
  지방 승하차를 이 저장소 그래프 노드(`node_id = 노선번호|역번호`)에 연결.
- `notebooks/05_OD분석.ipynb` — 모듈 함수를 그대로 호출 (기존 노트북 컨벤션),
  실행 출력 포함.
- 산출물 `data/od/processed/`:
  - `od_station_mapping.csv` — 역사_ID ↔ node_id 774건 (커밋됨)
  - `od_station_unmatched.csv` — 미매칭 10건 (커밋됨)
  - `node_weights.csv` — 1,094 노드 전체의 일평균 승차·하차 (커밋됨)
  - `od_daily_avg.csv`(13MB, 381,261 노드쌍) / `od_peak.csv`(8MB) — 용량 제외,
    `python od_analysis.py` 실행으로 재생성

### 3. 문서

- `docs/OD_데이터_조사.md` — 국내 OD 데이터 전수 조사 (즉시 다운로드 가능분과
  신청 필요분, 출처 URL 포함)
- `docs/OD_신청_가이드.md` / `docs/API_신청_가이드.md` — 추가 데이터 확보 절차

## 검증 수치 (2026-08-03 실행, 이 저장소 nodes.csv 기준)

| 항목 | 값 |
|---|---|
| 역사마스터 → 노드 매칭률 | **98.7%** (784역 중 774) |
| 미매칭 10건의 사유 | 전부 GTX-A (그래프 미포함 노선) — 실질 매칭 실패 0건 |
| 매칭 방법 | name_line 770 / coord_only 4 (개명·표기차, 좌표거리 ≤ 68m) |
| 7일 총 통행 (지하철 4자리 ID) | 43,856,579 |
| 매핑 후 통행량 보존율 | **98.64%** (손실은 GTX-A·마스터 부재 ID) |
| OD 노드쌍 | 381,261 (self-loop 735쌍·1.17%는 경로배정 시 제외 권장) |
| 노드 가중치 커버리지 | seoul_od 763 + boarding_data(부산·대구·대전) 228 = **991/1,094 (90.6%)** |
| 지방 승하차 역명 미매칭 | 0건 |

상식 부합: 최대 통행쌍 강남↔잠실(일평균 5,200여 명), 승차 상위 서울역·강남·잠실·
홍대입구, 지방 상위 서면·반월당 — 알려진 이용 순위와 일치.

weight_source=none 103개 노드: 광주 1호선(20)·부산김해경전철(21)·동해선(23)·
대경선(8) 등 승하차 데이터 미확보 노선 + 서울시 마스터가 커버하지 않는 수도권
외곽 코레일 역(23) + 자기부상철도(6).

## analyze.py 연계 제안 (기존 코드 수정은 저자 판단)

1. **승객가중 효율의 가중치 교체** — `analyze.py` 의 운행빈도 가중 대신
   `data/od/processed/node_weights.csv` 의 `boarding_daily_avg + alighting_daily_avg`
   를 노드 가중치로 사용 (수도권 96%+, 전국 90.6% 커버).
2. **단절 통행량 실측** — 역/구간 제거 시 `od_daily_avg.csv` 에서 해당 노드를
   지나는 O-D 쌍의 통행량을 합산하면 "몇 명의 이동이 끊기는가"를 실수요로 정량화.
3. **첨두 취약성** — `od_peak.csv` (주중 07~09시 vs 11~13시)로 시간대별 수요
   편차를 반영한 취약성 비교 가능. README 한계 두 번째 문장("시간대별 수요 편차")
   에 직접 대응.
4. **한계**: OD는 수도권만 실측 (지방은 승하차량만, 역간 OD는 STCIS 신청 필요 —
   `docs/OD_신청_가이드.md`). 7일치(7월 말) 표본이므로 계절 편차는 미반영.
